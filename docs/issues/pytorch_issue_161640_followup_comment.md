# PyTorch issue #161640 note

This draft is retired.

The current repo evidence is better used as supporting context on the TransformerLens issue thread, not as a new PyTorch follow-up by default.

If a future PyTorch follow-up is needed, start from:

- `docs/issues/transformerlens_mps_evidence_bundle.md`
